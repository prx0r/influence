# Handover — 2026-09-21 Session

## What happened this session

### 1. PowPowPow Code Review
- Full audit of powpowpow codebase (85 files)
- Identified: duplicate storage (core.py vs warehouse.py), 3 API servers, dead collector, no tests
- Warehouse is an empty shell — architecture exists but no data flows in
- SafeTrade L2 collector was dead for 30+ hours

### 2. Data Collection Fixed
- Started SafeTrade collector (PID 39946) — collecting 12 coins, 25 streams
- **Problem: dies on reboot** — this is a local box, not a server
- Need VPS for permanence (Hetzner at 138.199.223.35, but SSH key not installed)

### 3. GitHub Push
- Pushed powpowpow to github.com/prx0r/powpowpow
- 408 email .eml files + agents.md + all code
- Had to redact secrets from agents.md (Cloudflare API key was in it)

### 4. Telnyx Phone System
- API key works: stored in vault + .env (gitignored)
- Saved to `/home/box/influence/stevejobless/.env` (gitignored)
- **Number picked: +447822000272** (07822 000 272) — triple-0 + ABA rhyme
- UK mobile inventory on Telnyx is tiny: only 07822 (80%) and 07537 (20%) prefixes
- Created `find_gems.py` — scans numbers, scores by memorability patterns

### 5. Influence System
- Full audit: dash is LIVE, core is LIVE, cmail/domainchecker deployed, qp/law frozen
- **Secret leak fixed**: removed Telnyx key from git history + .gitignore stevejobless/.env
- Added 4 new MCP tools: phone.find_gem, phone.search, phone.owned, phone.find_gem
- Added 5 agent phone tools: send_sms, read_sms, make_call, hangup, list_calls
- Added unified webhook endpoint for Telnyx (voice + SMS routing)
- Fixed SMS webhook registration in telnyx_wire()
- Created identity registry (core/identity.py) — permanent, append-only, receipt-backed
- Created SETUP.md — documents the full pipeline

### 6. pow.systems Setup
- Added to Cloudflare (zone ID: aaff4cc5673927379ac64e32aef94bc7)
- Nameservers: gina.ns.cloudflare.com + pete.ns.cloudflare.com (user updated Porkbun)
- Email routing enabled: support@pow.systems → tradesprior@gmail.com
- Identity created in DB (slug: pow-systems, id: 1)
- Telnyx credentials stored in vault
- 2/8 slots ready (domain + email)

### 7. VPS Issue
- Hetzner VPS at 138.199.223.35 (hostname: debz)
- Root password reset: stored in VPS creds file
- SSH key NOT installed — can't connect from this box
- Old VPS at 51.222.81.2 works with existing key

## Architecture Understanding

### The flow
```
decide identity → buy domain → setup email → setup phone
→ setup socials → manage from dash → view phone → human tasks
```

### Influence components
```
core/identity.py     — permanent identity registry (slots: domain/email/phone/socials)
core/cmail/          — 16 connectors (14 functional)
stevejobless/        — business brain (jobs, quotes, invoices, channels)
qp/                  — law bridge, judges, authority, spend, calibration
law/acom/            — frozen QP kernel
dash/                — LIVE dashboard (127.0.0.1:8793, tunnel: influence.intelligentothers.xyz)
cmail/               — Cloudflare email worker (live)
domainnamechecker/   — name intelligence worker (live)
```

### MCP tools (19)
- identity.list, identity.get, identity.create, identity.slot
- phone.search, phone.owned, phone.find_gem
- phone.send_sms, phone.read_sms, phone.make_call, phone.hangup, phone.list_calls
- setup.status
- job.list, job.get, name.check, name.handles, biz.status, email.needs_reply

## Priority Next Steps

### P0 — Critical (do first)
1. **Install SSH key on Hetzner VPS** — go to hetzner.com console, paste the public key
   - Then update SSH config: `Host vps1` → `HostName 138.199.223.35`
   - Deploy SafeTrade collector to VPS for permanence
2. **Buy +447822000272 on Telnyx** — dashboard.telnyx.com → Buy Numbers → search
   - Get connection_id from Call Control app
   - Run: `POST /api/backend/pow-systems/telnyx/wire`
3. **Test the identity system** — run `identity.get slug=pow-systems` from MCP

### P1 — This week
4. **Deploy pow.systems website** — add A record in Cloudflare, deploy landing page
5. **Claim socials** — @powsystems on Twitter/X, GitHub org, YouTube channel
6. **Wire SafeTrade collector to pow-systems identity** — connect powpowpow data to dash
7. **Set up Telegram** — @powsystems channel

### P2 — Next week
8. **Dashboard identity tabs** — each identity as a tab with icon, monitor, content
9. **Phone integration in dash** — view SMS/calls from dashboard
10. **Human task queue** — wire identity tasks into dash task list

## Files to know
- `/home/box/influence/stevejobless/.env` — Telnyx API key (gitignored)
- `/home/box/influence/identities/pow-systems.json` — pow.systems identity
- `/home/box/influence/core/identity.py` — identity registry
- `/home/box/influence/SETUP.md` — setup pipeline docs
- `/home/box/powpowpow/` — data collection (collector dead, needs restart)
- `/home/box/safetrade/` — SafeTrade collector (last ran Sep 21 21:16)

## Credentials
- Cloudflare API: stored in /home/box/Documents/safe/cloudflare3
- Telnyx API: stored in vault + .env
- Hetzner root password: stored in VPS creds file
- GitHub PAT: stored in /home/box/.ssh/ (see vps-creds)
- SSH key for old VPS: `~/.ssh/vps_ed25519`
