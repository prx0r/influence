# TESTLOG 2026-09-18 — first actual logged testing (pq-style)

Runner: `experiments/live_targets.py` + `experiments/live_sweep.py`.
Every check logged JSONL to `runs/` (gitignored). Every finding carries a
QP receipt in `dash/receipts.jsonl`. Raw logs: `runs/targets-*.jsonl`,
`runs/sweep-*.jsonl`, `runs/tunnel-*.jsonl`.

## Live targets (LIVE=1, real domains, real DNS/HTTP observations)

Targets: sweep-alpha/example.com, sweep-beta/example.org. Both reconciled
`wired-needs-content`, domain+website READY from genuine resolution —
not fixtures.

| Check | Result | Receipt |
|---|---|---|
| seed-targets (2 added via chat /add) | PASS | receipt:680a944bdeb4bb7d |
| reconcile-live (2 inf, 7 tasks, 11 READY) | PASS | receipt:32e9531117e29ca6 |
| decide-all-tasks (7 tasks, 7-approve/1-hold mix, both journal paths) | PASS | receipt:2990dc811f8389c9 |
| resource-done (handles→READY, unknown slug 404) | PASS | receipt:9610253f91cd3b2c |
| autopilot (executed=0 skipped=0 verified=67%, conservative) | PASS | receipt:1c6c50808b9a0b57 |
| adversarial (400/409/404/evil-tools/unknown-receipt) | PASS | receipt:1c6c50808b9a0b57 |
| verify-chain (231 receipts re-settled, chain ok) | PASS | receipt:f7a1ecae41f4d588 |
| chat-agent (tasks/graph/products/hey/influencer) | PASS | receipt:5eed75be794f1a41 |

8/8. First run went 7/8 and caught a real bug (below).

## Public tunnel (edge → tunnel → brain)

| Check | Result |
|---|---|
| public-health, public-gate (403 sans token) | PASS |
| public-mcp (7 tools, live queue) | PASS |
| public-chat (urgency queue answers) | PASS |

4/4.

## Real bugs found by actual testing

1. **Chat infinite recursion** — bare `tasks` re-entered `handle_chat`
   with the same word (`maximum recursion depth exceeded`). Fixed by
   falling through to handlers instead of recursing. First run logged
   the FAIL with receipt:cdadd398839deb1a; rerun green.
2. **Cloudflare edge 403s Python-urllib UA** — tunnel checks failed
   auth-looking 403s that were edge bot-blocking, not our gate.
   Verified with curl UA swap. Fix: `User-Agent: influence-sweep/1.0`
   on all out-of-band test clients.
3. **Duplicate tunnel processes** — two cloudflared instances on the
   same named tunnel (one from another session). Same config, same
   backend, harmless, but noted: restart hygiene matters.

## Standing state

- Backend holds sweep-alpha + sweep-beta (labeled, keep for htask play).
- decisions.db holds the full present→decide trail from this pass —
  the agents tab shows it.
- Promotion (lineage/tournament) still designed, not encoded.
