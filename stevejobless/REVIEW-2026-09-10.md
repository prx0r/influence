# Review: agentic business stack — 2026-09-10

Two repos, one system. Voice agent is the other agent's lane; everything below is ours.

## Live right now (verified, not claimed)

| Piece | Where | Receipt |
|---|---|---|
| Email bus | `cmail.tradesprior.workers.dev` | D1 5 msgs, MCP 14 tools, drafts table, dry-run clean, vitest 10/10 |
| Brain + API | `steve.intelligentothers.xyz` (systemd + named tunnel) | 69/69 pytest, 7-key reconcile |
| Tradie pipeline | intake→job→quote→schedule→invoice→CSV | live quote £432.50, invoice SPARKY-*, calibration in stats |
| Comms loop | email→job, quote→cmail draft queued | draft `1789016652…` sitting in D1 |
| Channels | desk, slots, feed, agent profile, SKILL.md | feed 4 items, slots live, desk 200 |
| Backends | Telnyx/LiveKit-config/gcal modules + routes | honest MISSING states, stub paths tested |

## Gaps, ranked by what blocks a real pilot

1. **Credentials (blocks everything external).** WhatsApp Cloud API tokens, Telnyx key, Google OAuth, one domain's MX. Zero code needed — ~30 min of dashboard clicking with a sparky, SKILL.md scripts it.
2. **$5/mo Workers Paid** (blocks outbound email only). Flip same day as MX cutover.
3. **A real pilot business.** Calibration, photo-ask tuning, VAT — all need real job data.
4. **Voice runtime** (other agent). Our handoff: `/sip-config`, webhooks, slots, VOICE_COMPAT.md. Nothing pending on our side.
5. **Social inbox→lead** (IG DMs → jobs). Adapters exist; needs tokens from gap #1 first.

## Decisions needed from you

- [ ] Which domain migrates first + when MX flips (suggest `intelligentothers.xyz`, low stakes)
- [ ] Pilot sparky: who, and when's the 15-min onboarding session
- [ ] Telnyx vs Twilio confirmed? (built Telnyx; Twilio is a channels.py swap)
- [ ] Workers Paid: flip now or on migration day? (recommend migration day)

## Suggested next steps (our lanes only)

1. **Google Calendar read-write against a real account** — code's done, needs one OAuth grant to prove live freebusy (currently fallback-tested only).
2. **Social inbox→lead bridge** — IG DM webhook → jobs, same continuity rule as WhatsApp. Small build once tokens exist.
3. **Second business onboarding** — proves multi-tenancy (feed ranking across two kernels is the real test of the marketplace thesis).
4. **Outbound email send path** — uncomment SENDER, test to ourselves, leave Paid flip for migration day.
5. **Taxes lite** — VAT field on invoices + quarterly CSV. Only when a pilot has revenue.

## Risks

- **Meta policy/pricing squeeze** (researched 2026-09-10): mitigated by email-agnostic multi-channel + official-APIs-only + business-process framing. Not blocking.
- **Secret sprawl**: provider keys now in encrypted vault (good); `CMAIL_DRAFT_SECRET` in systemd drop-in + tunnel token in env file (acceptable, root-only, note for rotation).
- **Live DB is a single SQLite file** (`/root/stevejobless.db`): fine for pilots, needs backup story before real revenue (nightly copy to R2 — 10-line job).
- **AGPL contamination**: AIReceptionist/sara are patterns-only. No AGPL code in our tree — keep it that way (see SKILL.md traps).
